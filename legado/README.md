# Legado: versão Google Sheets + Apps Script

Backend antigo do Rachaê (planilha `Divisao_Contas_Casa` + Apps Script). Não é mais
usado pelo app — os dados agora ficam no Firebase (veja o README principal). Fica
aqui só como referência das regras de cálculo originais.
